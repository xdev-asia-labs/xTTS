{
  "servers": {
    "xtts-config": {
      "type": "http",
      "url": "http://localhost:3100",
      "description": "xTTS deployment configuration manager — view/update env vars, restart service, check status"
    }
  }
}
